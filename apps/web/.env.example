# =============================================================================
# Nexus AI Web — frontend environment template (application-local)
# Copy to apps/web/.env.local and fill in real values. NEVER commit .env.local.
# Placeholders only — no real keys, passwords, or tokens belong anywhere here.
# =============================================================================

# Base URL of the backend API (exposed to the browser; no secrets here)
NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"